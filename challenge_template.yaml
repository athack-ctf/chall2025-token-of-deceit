# Name should be "Challenge name" if only one flag, or
# "Challenge name - 1", "Challenge name - 2", etc. if multiple ones.
# Be careful of the spaces before and after the '-'
name: "Token of Deceit"
author: ""
category: <Web>
description: |
    We use JWT for authentication, packing all your user info into the token for secure, session-free logins (pretty awesome, right?).

    Once you're in, we decode your token to see who you are — no tricks, no sneaking past us!

    [Start your challenge instance](https://instancer.athackctf.com/chall2025-token-of-deceit/create)
    
value: 0

# Let's put the challenges as standard for now and we can script to change
type: standard


# Flags specify answers that your challenge use. You should generally provide at least one.
flags:
  - type: "static"
    content: "ATHACKCTF{trust_is_a_vulnerability}"
    data: "case_insensitive"

# If multiple flag challenge, put here the names of all previous challenges
# requirements:
#     - "Warmup"
#     - "Are you alive"

# Put hidden by default
state: hidden

version: "0.1"